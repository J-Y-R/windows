# 多媒体播放器 - 项目方案文档 (Project Proposal)

## 1. 项目背景

随着数字化音乐和播客内容的普及，用户需要一个轻量级、易用的桌面音频播放器来管理本地音乐文件和在线音频资源。本项目的目标是开发一个基于 Windows 窗体的多媒体播放器应用程序，支持本地音频播放、网络流媒体播放、播放列表管理和音频文件缓存等功能，综合运用《Windows程序设计》课程中学习的多项核心技术。

## 2. 功能需求分析

### 核心功能
| 功能模块 | 功能描述 | 优先级 |
|---------|---------|--------|
| 音频播放控制 | 支持播放、暂停、停止、上一曲/下一曲、进度拖拽 | 高 |
| 本地文件支持 | 打开并播放 .mp3/.wav/.flac 等常见音频格式 | 高 |
| 网络流媒体播放 | 通过 HTTP/HTTPS URL 播放网络音频流 | 高 |
| 播放列表管理 | 添加、删除歌曲，持久化保存播放列表到 SQLite 数据库 | 高 |
| 音量控制 | 滑动条调节音量 | 中 |
| 网络文件缓存 | 后台下载网络音频文件到本地缓存，支持断点续传 | 中 |
| 播放模式切换 | 顺序播放、单曲循环、随机播放三种模式 | 中 |
| 播放列表导出 | 将播放列表导出为 M3U 格式 | 低 |

### 非功能需求
- 响应式界面：后台下载不阻塞 UI 线程（使用 async/await 异步编程）
- 数据持久化：播放列表自动保存到本地 SQLite 数据库
- 健壮性：网络超时处理、文件不存在提示、异常捕获与日志

## 3. 技术选型理由

| 技术 | 选型理由 |
|------|---------|
| **C# / .NET 9.0** | 课程核心语言，最新 LTS 版本，性能优异，与 Windows 平台原生集成 |
| **Windows Forms (WinForms)** | 课程重点学习内容，成熟的桌面 UI 框架，控件丰富，开发效率高 |
| **NAudio 2.2.1** | 成熟的 .NET 音频处理库，支持多种音频格式和网络流播放，API 简洁 |
| **System.Data.SQLite** | 轻量级嵌入式数据库，无需单独安装服务，适合桌面应用的本地存储 |
| **异步编程 (async/await)** | 课程并发控制知识点，确保 UI 响应性，防止界面卡死 |

## 4. 系统架构设计

```
┌──────────────────────────────────────────────────────────┐
│                     MainForm (UI层)                       │
│  - 用户交互（按钮、列表、拖动条）                          │
│  - 事件处理与状态更新                                      │
└──────────────────────┬───────────────────────────────────┘
                       │ 依赖注入
         ┌─────────────┼─────────────┐
         ▼             ▼             ▼
   ┌──────────┐ ┌──────────┐ ┌──────────┐
   │PlayerSvc │ │DatabaseSvc│ │NetworkSvc│
   │(NAudio)  │ │(SQLite)   │ │(HttpClient)│
   │播放控制  │ │数据持久化 │ │文件下载  │
   │音频解码  │ │播放列表  │ │流媒体缓存│
   └──────────┘ └──────────┘ └──────────┘
         │             │             │
         ▼             ▼             ▼
   ┌──────────┐ ┌──────────┐ ┌──────────┐
   │音频文件  │ │ SQLite  │ │ HTTP 源 │
   │/网络流   │ │ 数据库   │ │ 服务器  │
   └──────────┘ └──────────┘ └──────────┘
```

### 项目文件结构
```
MediaPlayer/
├── Program.cs              # 应用程序入口点
├── MainForm.cs             # 主窗体（UI逻辑）
├── MainForm.Designer.cs    # 主窗体控件布局
├── InputDialog.cs          # 输入对话框
├── Models/
│   └── Song.cs             # 歌曲数据模型
├── Services/
│   ├── PlayerService.cs    # 音频播放服务（基于NAudio）
│   ├── DatabaseService.cs  # 数据库服务（基于SQLite）
│   └── NetworkService.cs   # 网络下载服务（基于HttpClient）
└── Project_Proposal.md     # 项目方案文档
```

## 5. 开发环境说明

| 项目 | 说明 |
|------|------|
| 操作系统 | Windows 11 (10.0.26200) |
| 开发工具 | .NET SDK 9.0.315, Visual Studio Code / Claude Code CLI |
| 编程语言 | C# 12 |
| 框架版本 | .NET 9.0-windows |
| 运行时 | .NET Runtime 9.0.315 |
| 包管理 | NuGet (NAudio 2.2.1, System.Data.SQLite.Core 1.0.119) |
| 版本控制 | Git (本地仓库) |

## 6. 项目仓库地址

> 本地开发仓库：`./MediaPlayer/` （Git本地仓库，共2次提交）

**提交记录：**
```
ba3fddd 添加.gitignore
6c3f2c4 初始提交：多媒体播放器项目
```

（注：本项目为课程作业，暂未推送至远程 Git 平台。项目源码位于 `MediaPlayer/` 目录下。）

---

*文档版本：1.0*  
*最后更新：2026年6月26日*
